<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:p="http://www.springframework.org/schema/p"
	xmlns:mvc="http://www.springframework.org/schema/mvc"
	xsi:schemaLocation="http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc-4.1.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.1.xsd">

	
	<context:component-scan base-package="controller, service, dao, dto" />

	
	<!-- Mapping -->
	<mvc:annotation-driven/>	
	<mvc:default-servlet-handler/>
	
	
	
	<!-- IRVR : 내부자원(JSP)의 상위경로, 확장자 설정 → 유지보수 하기 편함 -->
	<bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<property name="prefix" value="/view/" />
		<property name="suffix" value=".jsp" />
	</bean>
	
	
	
	<!-- 메세지 소스 IRVR 역활 빈 등록 -->
	<bean id="messageSource" class="org.springframework.context.support.ResourceBundleMessageSource">
		<property name="basenames">
			<list>
				<!-- 에러관련 메시지 처리
					message.error : src/message/error.properties → 무조건 확장자는 'properties'로 -->  
				
				<value>message.error</value>
				<value>message.label</value>
				<value>message.messages</value>
			</list>
		</property>
		<!-- property 파일 한글 UTF-8 인코딩 -->
		<property name="defaultEncoding" value="utf-8" />
	</bean>
	
	
	
	<!-- commons-fileupload api 사용 멀티파트 처리 : 크기 byte 단위 -->
	<!-- http://commons.apache.org/proper/commons-fileupload/ -->
	<bean id="multipartResolver"
		class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
		<property name="maxUploadSize" value="-1" /> <!-- 이미지 업로드 사이즈: -1 : 제한 없음<위험>. 가장 좋은건 1MB가 좋음  -->
		<property name="defaultEncoding" value="UTF-8" />
	</bean>
	
	
	
	<!-- AOP 설정 -->
	<!-- <mvc:interceptors>
		 
			//조건:요청주소패턴 /item/a /item/a/b 
			//<mvc:mapping path="/**/*.do"/> 
		
		//log관련 공통 인터셉터
		<mvc:interceptor>
			<mvc:mapping path="/**/*.do" /> *.do로 끝나는 모든 요청, *:모든파일, **:모든 폴더
			<bean id="measuringInterceptor" class="interceptor.MeasuringInterceptor" />
		</mvc:interceptor>
		
		
		//admin에 관련된 인터셉터 추가
		<mvc:interceptor>
			<mvc:mapping path="/admin/**" /> admin 으로 시작되는 모든 요청
			<bean class="interceptor.AdminLoginInterceptor" /> id 생략 가능
		</mvc:interceptor>
		
	</mvc:interceptors> -->
	
	
</beans>
